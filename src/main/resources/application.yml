server:
  port: 8888
spring:
  application:
    name: config-server

profiles:
  active: native
cloud:
  config:
    server:
      native:
        searchLocations:
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:https://localhost:8761/eureka}




